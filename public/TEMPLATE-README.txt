# Certificate template
#
# Save the "GIẤY CHỨNG NHẬN CƠ SỞ KINH DOANH KHÔNG MA TÚY" background artwork as:
#   public/certificate-template.jpg   (landscape 3:2, e.g. 1536x1024)
#
# The app overlays the business name (middle) + QR (bottom-left) at runtime,
# so this file must contain ONLY the static artwork (no business-specific text).
# If missing, the certificate view shows a notice instead of crashing.
